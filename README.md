# BacktestSaham
- To run the backtest yourself, run `script_runner.py`.
- The code can be further optimized to reduce the runtime of each backtest.
- A download link to the final backtest results is avaiable on the [website](backtestsaham.streamlit.app).